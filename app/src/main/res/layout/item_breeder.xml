<?xml version="1.0" encoding="utf-8"?>
<!-- item_dog.xml -->
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="Breeder"
            type="hr.algebra.pawprotectormobile.model.Breeder" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <TextView
            android:id="@+id/breederId"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{Breeder.idBreeder}"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_marginTop="8dp" />

        <TextView
            android:id="@+id/contact"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{Breeder.breederContact}"
            android:layout_below="@+id/breederId"
            android:layout_marginTop="4dp" />

        <TextView
            android:id="@+id/breederName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{Breeder.breederName}"
            android:layout_below="@+id/contact"
            android:layout_marginTop="4dp" />
    </RelativeLayout>
</layout>